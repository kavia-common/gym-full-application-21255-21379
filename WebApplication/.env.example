# PUBLIC INTERFACE
# React app environment variables must be prefixed with REACT_APP_ to be exposed to the client.
# Copy this file to ".env" and adjust values per environment (development, staging, production).

# Base URL of the Backend API service
# Example for local development with FastAPI running on port 8000:
REACT_APP_API_URL=http://localhost:8000
