# Example environment variables for local development.
# Copy this file to .env and adjust values for your environment.
# Do NOT commit your real .env to version control.

# Backend API base URL used by the frontend app.
# In CI and local dev, if not set, the app defaults to http://localhost:8000
REACT_APP_API_URL=http://localhost:8000

# Port used by the React dev server (Create React App honors PORT)
PORT=3000

# Prevent CRA from auto-opening a browser tab on start (set to none to disable)
BROWSER=none
